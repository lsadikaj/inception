
# Indique que toutes les directives qui suivent s'appliquent au processus serveur
# (mysqld = MySQL Daemon, le processus principal de MariaDB)

[mysqld]

bind-address = 0.0.0.0

port = 3306

datadir = /var/lib/mysql